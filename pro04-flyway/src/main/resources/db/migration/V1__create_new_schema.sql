-- And example of comment
    create table practice_spring_jpa_flyway_db.sample_entity (
        sample_entity_id bigint not null AUTO_INCREMENT,
        name varchar(255),
        primary key (sample_entity_id)
    );
